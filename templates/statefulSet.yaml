apiVersion: apps/v1
kind: StatefulSet
metadata:
  name:
  labels:
    app:
spec:
  replicas:
  serviceName:
  selector:
    matchLabels:
      app:
  template:
    metadata:
      labels:
        app:
    spec:
      containers:
      - name:
        image:
        command:
        ports:
        - containerPort:
